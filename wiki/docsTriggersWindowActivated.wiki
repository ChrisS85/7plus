= WindowActivated =

This trigger is activated when a specific window gets activated.
Use the [docsGenericWindowFilter WindowFilter] controls to limit the trigger to a specific type of window, or simply select "Active" to make it trigger on all windows.

= Tips =
If you need to do something with the window, simply use "Active" again in other WindowFilter controls.