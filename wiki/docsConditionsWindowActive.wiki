= WindowActive =
This condition is used to limit events to specific programs. It features [docsGenericWindowFilter window filter] controls that are used to limit the matching windows to certain types of windows. Usage of "Active" match type is obviously redundant here.

= Usage scenarios =
  * This condition is mostly used to limit hotkeys to specific programs, but of course there are also other possible uses.